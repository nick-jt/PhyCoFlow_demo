# FlowRecon-Bench (PoF 2026) — Session Handoff, 2026-09-12

For the next session. Read this first; it supersedes the "remaining work" list in
`HPC_MIGRATION_AND_REMAINING_WORK.md` §2 wherever they disagree. Deadline: **2026-10-30**.

## 0. State in one screen

| | |
|---|---|
| Paper | `Paper/pof2026/main.tex` — 28 pages, compiles clean (`cd Paper/pof2026 && ~/bin/tectonic -X compile main.tex`), 0 undefined refs/citations |
| TODOs left | **4**, all editorial and all need Nick (§5). No TODO needs compute. |
| Git | `main` = `origin/main` at `d5ab38b` (plus this handoff commit). Large uncommitted working tree — see §7 |
| Queue | empty — nothing in flight |
| Allocation | 561 / 750 GPU-h left on `bilr-dtai-gh` |
| Blocking decisions | 3D leakage claim; model-size sensitivity check; `.gitignore` for recalib JSONs (§5) |

## 1. Reading order

1. This file.
2. Auto-memory (loads when the session starts in `/work/hdd/bilr/ntricard/PhyCoFlow_demo`):
   `MEMORY.md` → `pof-integrity-key-invariant` is **mandatory reading** before touching any
   eval/aggregation code; also `sensor-draw-portability`, `pof-delta-campaign-status`,
   `deltaai-environment`.
3. `DELTA_STATUS_2026-09-08.md` §20–§31 — incident log and decisions, newest last.
4. `HPC_MIGRATION_AND_REMAINING_WORK.md` §3.4 (cluster gotchas) and §4 (protocol invariants).

## 2. Standing constraints (from Nick)

- **Don't retrain everything.** Transferred Kestrel checkpoints are canonical. They live at
  `/work/hdd/bilr/ntricard/Save_TrainedModel` (a *sibling* of the repo, reached by per-family
  symlinks from `0_demo_TurbulentCombustion/Save_TrainedModel/`).
- **Normal queue only**: partition `ghx4`, account `bilr-dtai-gh`, via `sbatch`. No interactive nodes
  (they cost more).
- **Protocol invariants** (never change): canonical sensor draw `helpers.build_sparse_condition`
  under `torch.manual_seed(seed*777+snap)`; ensemble noise `base=seed*131+si`,
  `torch.manual_seed(base*10000+k)`; **single samples** (not ensemble means) for spectra and
  galleries; per-channel reporting; TUNE/TEST = odd/even snapshots (cylinder: `--split-by position`,
  because its canonical frames are all even).
- Evals must export `JHU_SPLIT_MODE=block JHU_SPLIT_GAP=0`. **`helpers.py:261` defaults to
  `shuffle`** when unset; the eval driver guard refuses to run without the export.
- Nick has asked for work to be pushed to `main`. Commit trailer:
  `Co-Authored-By: ...` + `Claude-Session:` lines as given by the harness.

## 3. Results as the paper states them

### Kolmogorov 256², 1 % sensing (655 sensors), 50 test frames — `tab:kolm`
Ensemble-mean rel-L2 / single-sample rel-L2 / CRPS:

| Method | mean | single | CRPS |
|---|---|---|---|
| Geo-FNO (**budget-matched**, `kolmogorov2d_fullbudget`) | **0.417** | 0.417 | 0.287 |
| DMF-Gen (NFE 4) | 0.487 | 0.578 | 0.235 |
| SiT (50 steps) | 0.494 | 0.590 | 0.234 |
| S3GM (PC-DPS) | 0.498 | 0.700 | **0.231** |
| Latent FM (NFE 4) | 0.599 | 0.731 | 0.310 |
| MLP-RBF | 0.639 | 0.639 | 0.469 |
| Senseiver | 0.913 | 0.913 | 0.697 |
| IDW k=8 periodic / non-periodic | 0.531 / 0.546 | | |
| Nearest neighbour per./non-per. | 0.582 / 0.588 | | |
| Gappy POD r=80 | 0.680 | | |
| Training mean | 1.000 | | |

- Paired: Geo-FNO beats DMF-Gen by −0.071 [−0.079, −0.063]. DMF-Gen/SiT/S3GM are a **tie group**
  (not ranked). Seed study: pooled training-seed std 0.0041 (Kolmogorov), 0.0081 (cylinder).
- Budget-matched Geo-FNO (50k steps) chosen by Nick over the 4× run (0.385). Implemented as
  `fleet_select.ROW_SOURCE`; the seed study opts out (`apply_overrides=False`) because Geo-FNO's
  replicates were trained at 4×.
- Spectra, small-scale band k = 32–128, energy / truth: latent FM 1.17, SiT 1.72, S3GM 1.87,
  DMF-Gen 0.139, Geo-FNO 0.048, Senseiver 0.068, MLP-RBF 0.043, gappy POD 0.021, nearest
  neighbour 2.55. DMF-Gen NFE control (same frame/sensors/seed): 0.139 / 0.174 / 0.185 at
  NFE 4 / 16 / 50 → the deficit is architectural.
- Gallery frame single samples of the four leading rows: 0.57–0.82 (vs 0.42–0.50 averages).
- Protocol ablation: uniform lattice DMF-Gen 0.487 → 0.367, IDW 0.546 → 0.452; seen-trajectory
  ("leaky") split DMF-Gen 0.483; both combined 0.364. In 2D, **leakage is negligible (<3 %)**;
  layout dominates.
- Geo-FNO across the density sweep (sparsest → 10 %): 0.778 / 0.632 / 0.417 / 0.463 / 1.421
  (last at 10 %, marked off-scale in the figure).

**Measurement operators** (`tab:operator`, written 2026-09-11; K=8 generative, K=1 deterministic):

| Method | clean | σ=0.1 | σ=0.3 | 25 % slab | slab/clean |
|---|---|---|---|---|---|
| Geo-FNO | 0.417 | 0.421 | 0.457 | 0.554 | 1.33 |
| DMF-Gen | 0.487 | 0.492 | 0.525 | 0.660 | 1.35 |
| SiT | 0.494 | 0.501 | 0.555 | 0.709 | 1.44 |
| S3GM | 0.498 | 0.503 | 0.526 | 0.651 | 1.31 |
| Latent FM | 0.599 | 0.601 | 0.608 | 0.695 | 1.16 |
| MLP-RBF | 0.639 | 0.640 | 0.645 | 0.763 | 1.19 |
| Senseiver | 0.913 | 0.913 | 0.915 | 0.920 | 1.01 |

Channel dropout is implemented but **never run** (Kolmogorov observes one channel); the text says so.
Operator runs have no paired CIs — the text does not rank S3GM vs DMF-Gen under occlusion.

### Cylinder wake, 1 % mesh sensing (238/field), held-out Re {80, 250} — `tab:cyl`
Aggregate rel-L2: **gappy POD r=20 0.056** (p 0.051), Geo-FNO 0.093, Senseiver 0.145, latent FM 0.192,
DMF-Gen 0.221, SiT 0.328, MLP-RBF 0.388, S3GM 1.019 (documented sampler divergence, kept),
IDW 0.707. POD beats best learned by 0.037 [−0.042, −0.032]. GH200 re-score of the three
mesh-native rows moves them ≤ 0.006 (text quotes it; table keeps the original runs).

**Surface-to-field** (`tab:surface`, taps 32/64/128/360): Senseiver best (0.109 → 0.103), Geo-FNO
0.161 → 0.119, DMF-Gen 0.271 → 0.245; gappy POD 1.22 → 0.69; IDW/NN ≈ 2.1 (collapse). Ranking
inverts relative to volume sensing. Grid methods saturate at 62 cells († in table).

**Observe-u-only arm — has results, NOT in the paper** (P2-10):
`Save_TrainedModel/cylinder2d_uonly/` — Senseiver 0.148, DMF-Gen 0.250 (K8, NFE 4), classical
in `baseline_classical/classical_baselines_uonly_pod{20,80}_2d.json`. Decide whether to use it.

### JHU 3D isotropic turbulence (1.95 M points, U_x,U_z observed at 1 %, 50 snapshots, K=8) — `tab:jhu`
Aggregate rel-L2 (flagged, not headlined; per-channel is primary): latent FM **0.469** (best agg and
CRPS 0.244), SiT-point 0.545 (best observed channels 0.143 / 0.132), FNO3D 0.586, IDW 0.589,
DMF-Gen 0.593, S3GM normalized guidance 0.628, CoNFiLD published prior 0.635, Gen4Turb 0.670,
Senseiver 0.699, DeepONet 0.800, gappy POD 0.953. Unobserved channels ≈ 1.0 for everyone
(identifiability limit).

**Calibration** (90 % coverage, U_x, 0.1 / 1 / 10 % sensing, TUNE-fitted, TEST-frozen):
- DMF-Gen raw 0.403 / 0.560 / 0.755 → conformal 0.895 / 0.900 / 0.904; post-conformal
  spread–error 1.19–1.57 observed, 1.19 on p.
- Latent FM raw 0.553 / 0.654 / 0.661 → 0.897–0.898 observed, 0.911–0.924 unobserved;
  post-conformal spread–error 1.11 observed, 1.33–1.37 on p.
- Artifacts: `Paper/pof2026/recalib/conformal_calib_points_*.json` (DMF-Gen),
  `conformal_lfm_calib_points_*.json` (latent FM). Both carry `dump_dir`.

## 4. What landed in the last stretch (2026-09-10 → 09-11)

| Commit | What |
|---|---|
| `c30bb8d` | Geo-FNO Kolmogorov row = budget-matched 0.417 everywhere; authors block (Tricard, Wang, Chen, Deng — MIT MechE); placeholder citation dropped |
| `fcef0d3` | Spectra + Kolmogorov galleries re-dumped from the matched Geo-FNO (canonical idx_sum 22128955 verified) |
| `7305768` | Operator section + `tab:operator`; `sec:sensors` corrected re channel dropout |
| `d5ab38b` | Two-sided conformal calibration (latent FM beside DMF-Gen); conformal script fixes |

**Incident (fixed, 2026-09-11):** `src/run_conformal_all.sh` named outputs by density only; the first
latent-FM run overwrote DMF-Gen's conformal JSONs (git-ignored → no copy). Recomputed from the
intact dumps; every value the paper quotes reproduced exactly. The script now takes
`RD=<run dir> PREFIX=conformal_<model>_` and refuses a non-DMF-Gen `RD` without its own prefix.
This is the **fifth** instance of the "knob not in the key" bug class — see memory.

## 5. Open decisions — need Nick; do not act without an answer

1. **3D leakage pair 0.14 / 0.56** (intro ≈ `main.tex` "First, \emph{splits leak}", §`sec:leakage`
   (i), PROVENANCE TODO).
   - Facts: the model is **latent flow matching**. Source = archived pre-fix evaluations on the *old*
     617-consecutive-frame single cutout, single snapshot, different solver setting; not on this
     host (first appears in commit `6b58226`, `PAPER_PLAN.md`). The honest 0.56 must not be read
     against `tab:jhu` (latent FM 0.469 there; 0.56 is that row's coverage). "Factors of 2–4 …
     reorders methods" has **no tabulated source**.
   - Option A (recommended): **narrow** — state the archived setting beside the pair, drop the
     "2–4 / reorders" sentence, keep the 2D refutation as the measured result.
   - Option B: **re-measure** on the current `JHU_4cubes_stride100.h5` (4 cubes × 50 snapshots,
     stride 100; block split = cubes 0–2 train, cube 3 test). Design: retrain latent FM with
     `JHU_SPLIT_MODE=shuffle` (seeded 150/50), ~14 GPU-h + <1 h eval; honest arm is the existing
     0.469; IDW on both test sets as a frame-difficulty control (CPU); optional zero-training
     in-sample ceiling on existing checkpoints. ~15 GPU-h (latent FM only) / ~45 (+DMF-Gen, FNO3D;
     needed to keep any "reorders" claim). Code first: `eval_kolm_ensemble.py:187` refuses
     non-block; add a split knob keyed into filename + cache + protocol stamp, and make
     `fleet_select` exclude it. **Caveat:** stride 100 → nearest training frame is r ≈ 0.67, not
     the r = 1.00 near-duplicate the intro describes; the text would have to say "leakage at our
     snapshot spacing".
2. **Model-size sensitivity.** Nothing was shrunk this campaign (Save_config diffs are paths only);
   ~6.5 M parameter matching is protocol rule (ii), in the configs since `cf42d1b` (2026-08-25),
   target = DMF-Gen's own 6.51 M. Exposure: SiT-point 6.57 M vs smallest published SiT-S ~33 M;
   the S3GM cylinder divergence is attributed to the sampler "at this benchmark's parameter
   budget", which a reviewer can pin on size. Offered: retrain SiT and S3GM at published size on
   Kolmogorov to show the ranking is size-robust. Not yet priced.
3. **`.gitignore` line 31 (`*.json`)** ignores `Paper/pof2026/recalib/*.json`, the sources of the
   calibration numbers. Suggested exception `!Paper/pof2026/recalib/*.json`. Note `.gitignore`
   itself is untracked.
4. **Editorial TODOs** (the 4 remaining): abstract numbers + artifact DOI (`\todo` in abstract);
   PROVENANCE (item 1); acknowledgments + JHTDB providers; Zenodo/HF DOI + JHTDB redistribution
   terms. Also: no corresponding-author email; the DMF-Gen entry in `refs_extra.bib` lists six
   authors in a different order from the four on this paper — one is out of date.
5. **Check wording**: cylinder seed study — mesh-method replicates (DMF-Gen, Senseiver, MLP-RBF) ran
   on GH200 against an H100 canonical draw, so their "seed spread" includes sensor-draw variance
   (conservative; conclusion survives, label is wrong). DELTA §26. Verify the paper says so.

## 6. Tools and hazards

- **Read side:** any script turning fleet JSONs into paper numbers must use
  `src/fleet_select.load_canonical_fleet(ds, root)` (selects by payload protocol/n_obs/K, raises on
  ambiguity). Never filename-glob, last-file-wins.
- **Write side:** `src/artifact_guard.py` (`safe_write_json`, `exit_if_conflicts`) — every JSON write in
  `eval_kolm_ensemble.py` goes through it. Other writers (e.g. `conformal_recalib.py`, the JHU
  scripts) do not; new eval code should.
- **Generated table bodies — never hand-edit:** `tab_kolm_body.tex`, `tab_cyl_body.tex`
  (`src/figs_pof/make_2d_tables.py`), `tab_surface_body.tex` (`make_surface_table.py`, untracked!),
  `tab_operator_body.tex` (`make_operator_table.py`), `tab_crossre_body.tex`.
- **Figures:** galleries `src/figs_pof/fig_gallery_allbaselines.py --paper`; spectra
  `fig_spectra_kolmogorov.py`; density `fig_perf_vs_sensors.py`.
- **Sensor-draw portability:** cylinder mesh `randperm` at 23,800 points differs H100 vs GH200.
  Always compare `sensor_idx_sum`; reproduce another GPU's draw with `--sensor-indices-npz`.
- **Latent FM:** stage-2 checkpoints embed the stage-1 path — eval relocates it (`relocate_stage1`);
  `src/lfm_fixes.py` (vendored by Nick) must import (`cond_mode=image_norm`).
- **Runtime is the tell** for cache/resume bugs: a 2.5 h job finishing in 2 min resumed something
  it shouldn't have.
- **No fleet-wide universals** ("beats every row") until every row has landed; name the binding case.
- `git ls-files --error-unmatch <f>` before span-editing a script — several paper-feeding scripts are
  untracked and have no git copy.

## 7. Working tree (uncommitted, pre-existing — not from the last commits)

- ~321 modified files, mostly the Kestrel → Delta port: SBATCH headers
  (`gpu-h100`/`f2pde` → `ghx4`/`bilr-dtai-gh`) and data paths (`/projects/ammoniacomb/...` →
  `/work/hdd/bilr/ntricard/datasets/...`). Needed to run here, never committed.
- Real code changes also uncommitted, and current results depend on them: `src/Model.py`
  (`@torch.compiler.disable` on the KeOps kNN), `helpers.py` (+51, surface sensor pool),
  `helpers_baseline.py` (+31), `baseline_classical_2d.py` (+31), `recalibrate_spread.py` (+27,
  `--split-by position`), `dump_calib_points.py` (+22), `model_baseline.py` (+18),
  `train_pointcloud_ffm.py` (+15).
- 24 untracked, including surface-task code (`make_surface_table.py`, `add_grid_surface_indices.py`,
  `submit_cyl_surface_fleet_delta.sh`, `SURFACE_TASK_PREREGISTRATION.md`), `paired_ci_jhu.py`,
  `deferred_followup.tex`, submit scripts.
- Recommendation: commit in logical chunks (Delta port / surface task / tooling) — **ask Nick
  first**; it is 300+ files.

## 8. Dropped or never run

- SiT patch-4 arms (P2-9): no run dirs or JSONs exist — dropped.
- Channel-dropout operator: implemented, not run (single-channel regime).
- Non-completed jobs since 2026-09-10 (3116596 kolm_sweep_fleet timeout, 3116671 conformal,
  3122142/3 surface S3GM n32/n64, 3125236 op_smoke, 3127263/3127311 jhu_gallery) were all
  superseded by later successful runs; their results are in the paper.

## 9. Suggested order of work

1. Get Nick's answers to §5.1–5.3.
2. Apply the leakage decision (A: text only, clears PROVENANCE TODO; B: code knob → smoke → train →
   eval → write).
3. Commit the working tree (with Nick's OK).
4. Artifact packaging + reproduction dry-run (P2-13); JHTDB terms decide whether data ships or a
   regeneration recipe does.
5. Final consistency pass: every abstract/intro number against its table (`src/figs_pof/verify_sources.py`
   exists — check what it covers before relying on it).
